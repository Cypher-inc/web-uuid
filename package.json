{
  "dependencies": {
    "crypto-js": "^4.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
    "scripts": {
      "build": "esbuild index.js --minify --outfile=dist/bundle.js",
       "deploy": "gh-pages -d dist"
    }
  
}
